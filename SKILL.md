---
name: req2code
version: 1.1.0
description: "需求到交付全流程编排，支持单阶段执行并停靠：需求分析→PRD→HTML 原型→测试 Case→开发设计→前后端编码→联调验收，产物落盘、阶段门禁确认。触发：/req2code、$req2code、「从需求到交付」「走一遍全流程」「只做需求分析/PRD/原型/开发设计」「分析设计这个需求」「需求落地」「继续/迭代某需求」。"
---

# req2code：需求到交付全流程

把「需求调研结果 / 一句话原始需求」变成可交付的代码：需求分析 → 产品与交互设计 → 开发设计 → 前后端编码 → 联调验收。每个阶段的结论固化成文件，下一阶段按绝对路径读取，AI 与 AI、人与 AI 之间零信息损耗。

> 流程权威：本文件是唯一流程定义；[references/prompts.md](references/prompts.md) 仅提供可粘贴到会话的指令 payload，不另立流程。

## 启动自检（先于一切）

**任何要在本工作区/仓库落地的需求（含「分析设计一下」「看看怎么做」「实现个功能」）都适用本流程。开始任何分析、设计或实现动作前，先完成三项自检：**

1. **本会话是否已加载本 skill？** 未加载 → 先调用 `req2code` 再继续；不得在未加载状态下直接分析、设计或写码。技能不跨会话继承：新会话、上下文压缩后同样重新自检。
2. **是否存在进行中的流水线？** 检查 `{仓库}/docs/ai-coding/` 最近需求目录的 `_状态.md`。存在「进行中/待确认」状态 → 先报告状态并询问「续跑该需求还是新开」，不擅自继续。
3. **「开始 / 继续 / 确认 / 开始吧」不是编码许可**：先对照 `_状态.md` 判定当前阶段与门禁；只有双门禁（PRD 已确认 + 开发设计已确认）在产物中留痕后，才允许进入 Phase 6/7。

> 会话中断或切换时，续跑一律以 `_状态.md` 与产物文件为准，不依赖会话记忆。

## 核心原则

1. **文件即交接**：阶段产物必须落盘到 `{产物仓库}/docs/ai-coding/{YYYYMMDD}-{需求短名}/`，下游只读上游文件，不靠会话记忆。设计文档要能被"另一个 AI"直接拿去写代码，无需再问需求。
2. **仓库约定优先**：一切设计遵循仓库现有约定，不引入新框架、新风格；向后兼容优先；禁止凭空假设，设计必须可被仓库内实际代码验证。
3. **证据优先**：每个结论标注证据状态（有证据 / 无证据-观点驱动）；不确定项列入待确认清单，禁止静默假设。
4. **复用优先**：先查仓库复用清单，每个新增文件要回答"为什么不能复用现有 X"；新增依赖必须经用户确认。
5. **用户是决策者**：AI 负责复述、给选项、给建议；确认与拍板由用户完成。不伪造确认，不替客户拍板。
6. **编码前必须过门**：PRD v2 已确认 + 开发设计已确认，缺一不可；以产物中的确认留痕为准，不以会话记忆为准。

> 运行环境兼容：Claude Code / OpenCode / Codex / OpenClaw 均适用。外部 skill（prd-writing、grilling、anti-ui-slop 等）优先调用，**缺失或不可用时不阻塞流程**：按 [references/toolchain.md](references/toolchain.md) 的内置 rubric 降级，并在产物中标注 `Fallback`；同一会话首次降级时一次性告知用户缺失清单，不反复提醒。subagent 不可用时按 [references/collaboration.md](references/collaboration.md) 降级。

## 阶段总览

| Phase | 名称 | 输入 | 产出 | 停靠点 |
|---|---|---|---|---|
| 0 | 输入确认与仓库侦察 | 调研结果/原始需求 + 仓库路径 | `P0-输入与仓库侦察.md` + `_状态.md` | 仓库路径缺失时询问 |
| 1a | 需求理解复述 | 需求输入 | `P1-PRD.md` 第 0 节（5 要素 + 证据标注 + 待确认清单） | **必须用户确认** |
| 1b | 需求分析（PRD） | 确认后的理解 | `P1-PRD.md` v1（第 1-8 节） | 进入澄清 |
| 2 | 需求澄清（grill） | PRD v1 | `P1-PRD.md` v2 + 评审要点 + 确认状态 | **必须用户确认** |
| 3 | 交互设计（按需） | PRD + 参考源 | `P3-设计说明.md` → `P3-交互原型.html`（+ `.verify/` 实测证据） | 设计说明确认 + 原型确认 |
| 4 | 场景与测试 Case（默认必做） | PRD + 原型 | `P4-场景与测试Case.md` | 随 Phase 5 一并确认 |
| 5 | 开发设计 | PRD + 原型 + 仓库 | `P5-开发设计.md`（含决策记录 D1..Dn） | **必须用户确认才编码** |
| 6 | 后端开发（按需） | 开发设计 + 后端仓库 | 代码改动 + `P5` 实施状态回填 | 报告改动与验证方式 |
| 7 | 前端开发（按需） | 前端改动点 + 原型 + 前端仓库 | 代码改动 + `P5` 实施状态回填 | 报告改动与验证方式 |
| 8 | 联调与轻量验收 | 全部产物 + 运行环境 | `P8-验收记录.md` | 输出 bug 清单与回归结论 |

产物目录、文件模板与交接协议见 [references/artifacts.md](references/artifacts.md)；各阶段提示词模板见 [references/prompts.md](references/prompts.md)。

### 阶段裁剪矩阵

**规则：任何阶段跳过都必须显式获得用户确认（含 Phase 4）；跳过决策写入 `_状态.md`（状态=`已跳过`+原因），并在每次停靠报告中列出。** 编码前双门禁（PRD v2 已确认 + 开发设计已确认）不因裁剪取消。

| 需求类型 | 必需阶段 | 可跳过（需显式确认） |
|---|---|---|
| 全栈需求 | 0 → 1 → 2 → 3 → 4 → 5 → 6 → 7 → 8 | 无（4 必做） |
| 纯后端/接口需求 | 0 → 1 → 2 → 4 → 5 → 6 → 8 | 3、7 |
| 纯前端需求（接口契约已定） | 0 → 1 → 2 → 3 → 4 → 5 → 7 → 8 | 6 |
| 只做需求分析/PRD | 0 → 1 → 2，停靠 | 3-8 |
| 只出原型 | 0 → 1 → 2 → 3，停靠 | 4-8 |

## 触发与路由

用户明确说"只做需求分析 / 只要 PRD / 只出原型"等单阶段意图时，只执行对应阶段并停靠。

裸调用 `/req2code`（无明确意图）时：

1. 检查当前工作目录的 `docs/ai-coding/`，读取最近需求目录下的 `_状态.md`（无状态文件时按文件存在情况推断并补建），列出各需求阶段状态；
2. 只做状态诊断并列出下一步选项（如"已有 PRD v2，可继续交互设计"），**不自动执行任何阶段**；
3. 无产物时询问：需求输入在哪、前后端仓库路径（无仓库走绿地模式）、从哪个阶段开始。

---

### Phase 0：输入确认与仓库侦察

1. **先建实体（防中断丢失）**：创建产物目录 `{产物仓库}/docs/ai-coding/{YYYYMMDD}-{需求短名}/` 与 `_状态.md` 骨架（阶段全表 + 最后更新日期），再开始侦察；长任务被中断后凭 `_状态.md` 即可续跑。
2. 收集需求输入：调研结果 / 原始需求、需求方是谁、谁有权拍板、有无截止时间与合规约束。缺失的关键输入（如"调研结果在哪"）直接问；输入是网页/竞品资料时可用 Defuddle 提取正文。
3. 确认仓库路径：后端仓库、前端仓库、产物仓库（默认需求所属主仓库，或用户指定）。跨仓库时记录全部绝对路径。
   - **绿地模式**：无现有仓库时，与用户确认技术栈、目录结构、编码与 UI 规范后写入 `P0`；后续所有"仓库约定优先"以 `P0` 为唯一约定来源，参考源改为用户提供的截图/URL 或 anti-ui-slop 方向设定。
4. 按 [references/repo-recon.md](references/repo-recon.md) 侦察：技术栈版本、分层结构、命名/异常/鉴权/接口风格、DB 访问方式、**多租户与数据可见性边界**、**复用清单**、**前端 UI/UE 规范**；读取仓库 AGENTS.md / CLAUDE.md / README 中的编码规范。绿地模式跳过侦察，改为在 `P0` 中记录用户确认的技术栈与规范。
5. 产出 `P0-输入与仓库侦察.md`（模板见 artifacts.md），更新 `_状态.md`，向用户报告侦察结论与待确认项。

### Phase 1a：需求理解复述（必须确认）

在 `P1-PRD.md` 第 0 节用自己的话复述 5 要素：**背景 / 使用角色 / 场景 / 最终目的 / 需求边界**。要求：

- 每条标注 `[有证据]`（引用调研原话/数据/现象）或 `[无证据-观点驱动]`；
- 列出「待确认清单」：所有 AI 推断、歧义、缺失信息显式提问，禁止静默假设；
- 用户逐项确认或修正；用户回答不了的记入「待客户确认」，不替客户拍板。

**用户确认后才进入 Phase 1b。**

### Phase 1b：需求分析（PRD）

按 [references/prompts.md](references/prompts.md) 的「产品需求模板」生成 `P1-PRD.md` v1，固定 8 段：问题背景与证据 / 目标与成功指标（量化+反指标）/ 目标用户 / 使用角色权限与触点 / 核心场景与用户旅程 / 需求清单 P0-P2（编号 R1..Rn）/ 明确不做 / 边界与约束。优先调用 prd-writing skill，不可用时用内置模板与 rubric。

### Phase 2：需求澄清（grill）

切换到 grill 模式：优先调用 grilling skill，按其轮次机制执行（一轮问完当前 frontier，等回答后再进入下一轮）；grilling 不可用时按内置 6 个追问维度逐条挑战（目标可量化吗？场景真实吗？边界清晰吗？优先级对吗？约束漏了吗？证据充分吗？），一次最多问 3 个问题。两种方式共同要求：每个问题给出 2-4 个选项与推荐答案，不允许开放式提问。grill-me / grill-with-docs 仅用户手动触发（`disable-model-invocation`），模型不自动调用，可用性判定与降级见 toolchain.md。澄清问题编号 `Q1..Qn`（只增不复用）。根据回答修订为 PRD v2，并维护：

- **确认状态**区块：每条结论三态标记 `AI推断 / 用户确认 / 客户确认`；未确认项不得默认通过、不得向下游传递；
- **评审要点**：给客户/产品/UI/测试的确认要点与待确认问题清单（供用户线下评审）。

**PRD v2 必须用户确认后才进入 Phase 3。** 用户需要正式评审物料时，按 artifacts.md 产出 `P2b-评审确认包.md`；用户确认时按「停靠点与防失控」的确认落盘三件套同步留痕。

### Phase 3：交互设计（HTML 原型）

1. **先定参考源并显式确认**（无参考源不允许凭空发挥）：默认目标前端仓库；其次用户指定的参考仓库 / 截图 / URL。**用一句话向用户声明选定的参考源并等其确认或改指**，不得自动扫描后静默选定；没有参考源时走 anti-ui-slop / frontend-design 方向设定并请用户确认。
2. 按 [references/prompts.md](references/prompts.md) 的「交互设计模板」输出 `P3-设计说明.md`：信息架构 / 页面清单（映射 Rn）/ 组件清单 / 交互状态矩阵 / 响应式断点 / **风格来源与 token 提取映射表**（色板/字号/间距/圆角/组件与交互模式）。**用户确认设计说明后才写 HTML。**
3. 实现 `P3-交互原型.html`：单文件、内联 CSS/JS、无外部依赖、双击可运行；复刻参考源视觉语言，不引入仓库外新风格；覆盖全部交互状态（加载/空态/错误态/禁用态）；结构与 class 命名清晰，便于前端按组件落地。
4. **实测与自审**：用浏览器实际打开原型，走查全部交互状态，截图存入产物目录 `.verify/`，记录控制台错误；用 anti-ui-slop 的 finish gate（或内置 rubric，标 `Fallback`）自审并整改（反通用模板感、交互状态完整、响应式到位、可访问性达标）；交付附一致性自检（与参考源一致项 / 有意偏离项及原因）。
5. **原型经用户确认后才进入 Phase 5。** 用户要求精准修改时，指定文字、元素的 class/id 修改。

### Phase 4：场景与测试 Case（默认必做）

默认执行：基于 PRD 与原型生成 `P4-场景与测试Case.md`：主/异常/边界场景、前置条件、操作步骤、预期结果、对应需求编号 Rn，作为 Phase 8 验收依据。全栈/跨服务需求不可跳过；其他需求如要跳过必须经用户显式确认并记入 `_状态.md`。

### Phase 5：开发设计

按 [references/prompts.md](references/prompts.md) 的「开发设计模板」输出 `P5-开发设计.md`，固定 6 段：

1. 业务流程图（mermaid flowchart + sequenceDiagram，标注本次改动点）
2. 数据结构变化（新增/修改/删除表与字段、约束、索引、迁移方案、存量数据处理）
3. 接口设计（方法、路径、请求/响应字段表、错误码表、鉴权、幂等性；破坏性变更标注"不兼容"）
4. 后端改动代码结构（新增/修改/删除文件清单、模块边界、**复用点与不复用理由**、影响面与风险）
5. 前端改动点（页面/组件/接口调用/状态管理，逐一映射到 HTML 原型对应界面与 Rn）
6. 关键决策摘要（800 字内，3-5 个决策及理由）

约束：一切设计遵循仓库现有约定；向后兼容优先；禁止凭空假设；文档要能被"另一个 AI"直接拿去写代码。补充三条：

- **最简方案优先**：默认路径是扩展现有实体/接口/组件；每张新增表、每个新增接口/任务/依赖都要逐项回答"为什么不能复用或不新增"，并在关键决策摘要中给出「最简方案 vs 推荐方案」的取舍对比；禁止未经对比直接引入新存储/新任务/新组件。
- **可行性结论必须有证据**：凡"做不到 / 有代价 / 不支持"的结论，必须给出语法或机制级证据（代码位置、PromQL/框架语义），不得以未经验证的判断砍需求；证据不足时列为待验证项，而非既定结论。
- **决策记录**：关键决策编号 `D1..Dn`（结论 / 确认状态 / 影响），随用户确认同步更新。

**用户确认后才允许编码。**

### Phase 6：后端开发

进入前核对：`P1-PRD.md` 状态=已确认 且 `P5-开发设计.md` 状态=已确认 且 `_状态.md` 对应行一致；不一致先修复再实施。

按开发设计的后端改动清单在仓库内实施：遵循仓库分层与编码规范；复用优先，先检索相似实现；新增依赖/工具必须列出并经用户确认；若发现设计不可行，回 Phase 5 更新设计再实施。每批改动报告：改了哪些文件、如何验证、有无偏离设计。完成后回填 `P5-开发设计.md` 的实施状态。

### Phase 7：前端开发

按「前端改动点」+ HTML 原型 + 接口契约实施，五条铁律：

1. 严格遵循当前工程代码风格、结构与既有 UI/UE 规范，不引入新 UI 库/新风格；
2. 严格复刻原型的交互与全部状态（加载/空态/错误态/禁用态）；
3. 接口调用严格按设计文档契约（路径/字段/错误码处理），不自行调整；
4. 复用优先：优先扩展现有组件与样式方案，不重复实现；
5. 仓库无既有规范时，先产出一页风格基线请用户确认再实施。

完成后回填 `P5-开发设计.md` 的实施状态。

### Phase 8：联调与轻量验收

1. 前置：确认本地环境可启动（前后端启动/打包命令见 [references/repo-recon.md](references/repo-recon.md) 第五节）、有可用账号与登录态；缺失时先请用户提供，不自行造环境。
2. 用 Chrome DevTools MCP 联调：复用 Chrome 登录态与现场环境，查看接口请求/响应、控制台错误与 DOM 状态，验证悬浮/点击/抽屉/视图切换等交互；改代码后刷新页面对比效果。
3. 对照原型与验收项走查，逐条核对需求编号 Rn 的实现情况；执行 `P4-场景与测试Case.md` 用例。
4. 输出 `P8-验收记录.md`：验收项清单（编号 `A1..An`，含 Rn）、通过/失败、bug 清单（编号 `B1..Bn`，复现步骤、截图、报错、影响面）、回归结论、遗留项与建议。
5. 可用 code-review 审查改动（含重复实现检查）；疑难 bug/性能回退走 diagnosing-bugs；安全敏感改动建议走 security-audit。

**环境不可用时的降级**：产出离线自审（编译/静态走查/回归差异分析）+ 可执行联调清单，验收项标注「待环境」，**不得宣称验收通过**；环境恢复后按清单补跑并回填。

## 停靠点与防失控

- 编码前双确认：PRD v2 已确认 + 开发设计已确认，缺一不可。
- **确认落盘三件套**：用户每次确认时立即同步三处——① 产物头部状态（如 `状态：已确认`）；② 产物「确认状态」表（确认人/时间）；③ `_状态.md` 对应行（状态/确认人/日期）。只口头确认不落盘视为未确认。
- **停靠报告固定格式**：产物路径 + 本次确认项 + 被裁剪/跳过阶段 + 阶段↔文件映射 + 门禁状态 + 下一步选项；不自动越界执行。
- 上游升版后，回填所有下游产物「输入来源」的版本号（如 PRD 升 v8 后，`P5` 的输入须同步为 v8）。
- 无参考源不写原型；设计说明未确认不写 HTML；原型未确认不进入开发设计。
- 未确认项（含待客户确认）不得默认通过、不得向下游传递。
- 需求变更必须回 Phase 1/2 更新 PRD，并标注受影响的下游文件（原型/开发设计/代码/验收）。
- Rn 编号只增不复用：需求废弃时保留编号并标 `R3(已废弃)`，禁止重排或复用编号。
- 产物不写密钥/凭据/生产敏感数据，示例一律脱敏。
- 每阶段结束同步更新 `_状态.md`（含"最后更新"日期），只报告产物路径与下一步建议；批量执行仅在用户显式要求时进行。

## 交付前自检清单

按门禁分组核对，过门时只核对对应组：

**需求门禁（Phase 2 后）**
- [ ] 5 要素复述已确认，待确认清单无遗留未答复项
- [ ] 结论均有证据标注，AI 推断项已显式标记
- [ ] 确认状态三态标记完整，未确认项未向下游传递
- [ ] Phase 4 是否执行已明确（跳过需用户确认并记入 `_状态.md`）

**原型门禁（Phase 3 后）**
- [ ] 参考源已声明并经用户确认，token 映射表经用户确认
- [ ] 原型覆盖全部交互状态，浏览器实测完成，证据在 `.verify/`，finish gate 方式已标注

**编码门禁（Phase 5 后）**
- [ ] 开发设计含复用点与不复用理由、最简方案对比、可行性结论有证据；接口契约完整（字段/错误码/鉴权/幂等）
- [ ] 决策记录 D1..Dn 完整；无新增未确认依赖；设计遵循仓库现有约定
- [ ] 需求编号 Rn 从 PRD 贯穿到原型/接口/改动点；双门禁确认已在产物留痕

**交付门禁（Phase 8 后）**
- [ ] Rn 逐条核对实现情况，验收项与 bug 清单齐全（A/B 编号）
- [ ] 每阶段产物已落盘且下游按绝对路径引用，`_状态.md` 与产物状态一致、已更新

## 多会话协作（内置，支持降级）

主会话是需求和决策的唯一事实来源，负责需求澄清、关键决策、范围控制与最终验收；前后端/测试会话只负责执行，结论按固定格式（结论/影响文件/待决策项）同步回主会话。subagent 可用时按结构化 prompt spawn；不可用时提示用户开辅助会话并回传结论。详见 [references/collaboration.md](references/collaboration.md)。

## 外部 skill 与 MCP 集成

prd-writing（PRD 结构）、grilling/grill-me（需求澄清）、anti-ui-slop（UI finish gate）、frontend-design（视觉方向）、implement（按规格实施）、code-review、security-audit、diagnosing-bugs、draw.io（流程图）、Chrome DevTools MCP（联调）、Defuddle（网页正文提取）。调用点与缺失时的降级 rubric 见 [references/toolchain.md](references/toolchain.md)。
